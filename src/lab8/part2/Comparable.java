/* Lab 8: Overloading, Interfaces & Hierarchy */
package lab8.part2;

// The comparable interface to allow object comparison.
public interface Comparable<Animal> {
    // Interface methods.
    int compareTo(Animal animal);
}
